export type Item = {
  name: string;
  amount: number;
};

export type Movie = {
    id: number;
    title: string;
    director: string;
    year: number;
    image: string;
    description: string;
};

